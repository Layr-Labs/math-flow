{
  "id": "math-flow/validity-judgment-v2",
  "description": "An immutable validity-only primary judgment with exactly one assessment and one derived routing finding per declared claim.",
  "requiredArtifactRoles": ["judgment-report", "judgment-dependency-packet", "judgment-record"],
  "schemas": [
    "protocol/schemas/validity-dependency-packet.schema.json",
    "protocol/schemas/validity-judgment.schema.json"
  ],
  "reducer": null
}
