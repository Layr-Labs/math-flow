{
  "id": "math-flow/flat-json-v1",
  "description": "Example profile containing one structured projection.json artifact with verdicts, cumulative state, and credit.",
  "requiredArtifactRoles": ["flat-projection"],
  "schemas": ["protocol/schemas/projection.schema.json"],
  "reducer": null
}
