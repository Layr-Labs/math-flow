# BSSC accounting-topology fixed-K1 follow-up

This follow-up evaluates six independent Builder V9 transitions from the same
empty state and exact K1 validity/evidence input.

## Required accounting topology

```text
Canonical problem
└── narrow code-induced structural-converse portfolio
    ├── finite-block dependence-balance / structure-preserving single-letterization
    └── entropy/copy obstruction / BSSC-specific posterior-consistency repair
```

- The two leaves need independently updateable accounting parameters.
- The parent must express their narrow shared code-induced activation gate.
- Root siblings are under-nested; a generic all-converse umbrella is overbroad.
- The two accepted epistemic components should be separately reusable
  intermediate results with their mathematical dependency represented as a
  result dependency, not accounting ancestry.
- The parent normally has no direct result links; the leaves are the direct
  programs updated by the contribution.

## Validity modality

The controlling K1 qualification is that exact binary-posterior and other
channel-specific consistency constraints **may exclude** the witness. Every
surface must remain possible/unknown. `Can exclude`, `does exclude`, `excludes`,
or a residual-work premise that assumes exclusion is an unsupported
strengthening.

## Control validity

- Every entity appears in exactly one operation array.
- New entities use one `topologyOperations` create operation.
- Existing root synthesis uses one content operation.
- `directProgramIds` equals the exact union of result program links.
- Governed retries may repair rejected attempts, but first-attempt and final
  outcomes are scored separately.
