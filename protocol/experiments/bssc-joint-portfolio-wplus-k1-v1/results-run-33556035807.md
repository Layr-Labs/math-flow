# Joint portfolio/W+ K1 result — run 33556035807

Run: <https://github.com/Layr-Labs/math-flow/actions/runs/33556035807>

- Branch head: `35801ad2b54e400d43a7693daeae160ee986d86a`
- Subject: `c70e1829a7c6a2a8cb8cfc2383f8abf825ac5ea6`
- Seed: `1729`
- Publication: forbidden; no projection state was published
- Outcome: failed; K2 remains gated

## Provider execution

The governed stage made two OpenRouter calls. A third automatic attempt was
blocked before transport by the experiment's total-token preflight.

| Attempt | Finish | Prompt tokens | Completion tokens | Reasoning tokens | Cost |
| --- | --- | ---: | ---: | ---: | ---: |
| 1 | `length` | 39,906 | 16,000 | 3,624 | $0.2597635 |
| 2 | `stop` | 39,961 | 6,416 | 3,581 | $0.0722841 |
| Total |  | 79,867 | 22,416 | 7,205 | $0.3320476 |

Attempt 1 exhausted the 16,000-token completion ceiling and was rejected as
length-truncated. Attempt 2 returned complete structured JSON but failed trusted
validation because its `programs` array included `root`, which is an existing
program rather than a newly created program. The third attempt never reached
OpenRouter: the harness's conservative reservation of request bytes plus the
completion ceiling exceeded `maximumTotalProviderTokens`.

The terminal diagnostic currently describes that pre-transport budget rejection
as an uncertain transport outcome because the governed adapter wraps every
transport-callback exception at the same boundary. That wording is inaccurate
for this case; the captured call and usage records prove that only two provider
calls occurred.

## Empirical topology result

Attempt 2 proposed seven rows in `programs`: the illegal duplicate `root` plus
six new programs.

1. matching achievability;
2. matching converse;
3. an alternative converse route;
4. the code-induced converse parent;
5. the channel-specific-refinement leaf; and
6. the structure-preserving-reduction leaf.

The important positive result is that the desired local subtree appeared
correctly inside the proposal: one code-induced-converse parent with the two
independently estimable leaves, and each fixed intermediate result was placed on
the appropriate leaf.

The decisive regression is scope expansion. From an empty root, the judge
treated the supplied terminal contract and complete-W+ responsibility as a
request to invent a broad plausible research portfolio. It created unrelated
achievability and alternative-converse packages even though the fixed results
did not directly change those packages. The precommitted K1 target instead
requires exactly the narrow three-program subtree affected by this submission.

## Provider-free diagnostic replay

For diagnosis only, trusted local code removed the illegal duplicate `root`
program row and removed the two redundant ancestor `relatedProgramIds` links.
No model call was made. The repaired six-new-program candidate reduced to a
valid knowledge and W+ state, but failed these four hard relational assertions:

- `k1-adds-exactly-three-programs`;
- `k1-creates-exactly-three-program-operations`;
- `k1-creates-one-narrow-root-child`; and
- `k1-nests-two-leaves-under-new-root-child`.

That diagnostic state had `W+ = 3517.2524697289275857610152691308`
competent-human-researcher hours. This is not an accepted estimate, not a live
state, and not evidence of numerical calibration; it is only the deterministic
total implied by the rejected candidate's primitives.

## Interpretation and next revision

The experiment does not support adopting the current joint prompt. It does show
that accounting context can recover the intended independent leaf packages,
but the same context currently encourages global portfolio completion.

A next candidate should preserve the joint accounting information while making
the locality boundary explicit:

- `programs` contains newly created programs only; root is represented only in
  the boundary and W+ annotation arrays;
- create only the smallest program subtree whose work policy or residual work
  is directly changed by the fixed results;
- do not instantiate unaffected achievability, fallback, or alternative-route
  packages merely because the root terminal condition will eventually require
  them;
- link each result only to the narrowest program whose own residual work it
  directly changes; ancestry is not a related-program link;
- keep the complete root W+ estimate, but distinguish existing/root accounting
  coverage from permission to expand topology; and
- correct the harness so a pre-transport budget refusal is reported separately
  from an uncertain post-dispatch outcome.

Do not dispatch K2 or another K1 call until those protocol issues are revised
and the provider-free gate is revalidated.
