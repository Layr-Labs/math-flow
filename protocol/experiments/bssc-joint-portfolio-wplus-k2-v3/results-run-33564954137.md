# Results: run 33564954137

GitHub Actions run:
<https://github.com/Layr-Labs/math-flow/actions/runs/33564954137>

This was an unpublished, publication-forbidden K2 joint program-topology and
post-submission W+ experiment at commit
`11d027ad50b4f98ed243d73f24753ea7aeeafc27`. The production job was
skipped; no projection, catalog entry, or UI state changed.

## Topology outcome

The first provider response passed trusted reduction and all 13 precommitted
relational assertions, including all three advisory checks:

```text
root
├── program-bssc-code-induced-converse
│   ├── program-bssc-code-induced-converse-channel-specific-refinement
│   └── program-bssc-code-induced-converse-single-letter-reduction
└── program-bssc-uv-product-branchwise-additivity
    ├── result-uv-average-product-additivity
    └── result-uv-branchwise-symmetry-specialization
```

The judge treated the two K2 results as one dependency-ordered theorem chain
governing one continue-or-prune decision. It created one new root child rather
than one program per result. The second K2 result records dependencies on the
first K2 result and
`result-bssc-coarse-entropy-copy-refinement-no-go`, but the new program is a
sibling of the K1 portfolio because mathematical use of a result is not
conditional-pursuit ancestry.

Every K1 program identity and content object was preserved. The transition
refreshed root synthesis but did not rewrite either the K1 parent or its
channel-specific leaf.

## Live W+ outcome

Every K1 primitive remained exact:

| Program | Direct hours | Conditional incidence |
| --- | ---: | ---: |
| code-induced converse | 100 | 0.603 |
| channel-specific refinement | 900 | 0.75 |
| single-letter reduction | 750 | 0.85 |

K2 added or changed only:

| Program | Before direct hours | After direct hours | Conditional incidence |
| --- | ---: | ---: | ---: |
| root | 3600 | 3500 | — |
| UV product/branchwise additivity | — | 0 | 1 |

The judge explained that the accepted results completely settle the stated UV
blocking package, so its post-submission direct residual work is zero. It
reduced root direct residual work by 100 hours because the theorem prunes an
unrepresented blocking route while leaving the broader exact-capacity problem
open.

Trusted reduction therefore changed total live W+ from 4451.7375 to 4351.7375
competent-human-researcher hours:

```text
3500 + 0.603 × (100 + 0.75 × 900 + 0.85 × 750) + 1 × 0
= 4351.7375
```

The 100-hour decrease is a change in the live post-submission state A=W+. It is
not submission credit D. Credit still requires a same-world no-access W- state
and the trusted difference D=W-−W+.

## Active-only fix

The provider accepted on attempt one. Every created program was active, and
the fixed created results were active. The V2 schema/reducer mismatch that
forced a retry in K1 did not recur.

## Context and usage

- Provider calls: 1
- Prompt tokens: 47,123
- Completion tokens: 4,134
- Total tokens: 51,257
- Reasoning tokens: 2,888
- OpenRouter-reported cost: $0.159146
- Base knowledge state: 10,424 UTF-8 bytes
- Base live W+ state: 2,704 UTF-8 bytes
- Fixed semantic packet: 7,640 UTF-8 bytes
- Accepted claim assessments: 3,830 UTF-8 bytes
- Submission evidence: 45,454 bytes across five files

The serialized request was comfortably below the 768,000-byte experiment
ceiling. This single K2 point does show the expected sequential context growth
relative to K1, but it is not a long-horizon scale result.

As with K1, these numerical values are one judge sample. They are not
calibrated estimates and do not measure repeated-judge sampling variance.
