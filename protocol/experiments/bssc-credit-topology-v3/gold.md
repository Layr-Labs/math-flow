# BSSC generalized accounting-topology K2/K3 holdout

This is a precommitted relational gold standard for two sequential transitions
from one frozen, fully accepted K1 state. It is not an exact expected JSON state.

## Fixed K1 base

- The fixture is the accepted refined-accounting seed-2718 state from unpublished
  run 33457244165, with state digest
  `sha256:0f1e8c9399c977ce92b38a6617c3981858adf1123bb04c258d2eb3e6d9218846`.
- Its narrow code-induced dependence-balance parent and two leaf programs remain
  stable unless later accepted evidence genuinely changes their topology.
- Mathematical use of a K1 result by later work does not make the later program
  a child of the K1 portfolio.

## K2 — `f236017c62c67ce4218c1f81ea34134f0954b556`

- Create exactly one relaxed UV-scalar tensorization program as a child of root,
  outside the fixed K1 portfolio.
- Do not create a generic converse parent or a unary wrapper around the UV
  program.
- Represent the general averaged-scalar product-additivity theorem separately
  from the dependent receiver-skew/branchwise/BSSC specialization. Both results
  belong to the one UV program; result separation does not imply program
  separation.
- Express mathematical reliance through result dependencies. In particular,
  the BSSC specialization may depend on the general UV result and the narrow K1
  result that supplies the accepted BSSC support fact; this does not create
  accounting ancestry.
- Preserve every accepted qualification: the result is scalar-only,
  non-frontier, not a theorem about the full UV region or capacity, and the
  contextual stronger-bound certification is not a premise.
- Introduce every new program and result exactly once through topology create
  operations. Refresh existing root synthesis through a content operation.

## K3 — `14889884ae6ac1f80cc56485e7acf1b0b2cb6ae9`

- Reuse the exact K2 UV program and semantic result IDs. Create no new program,
  unary wrapper, or duplicate result for substantially the same theorem chain.
- Consolidate the additional accepted evidence and claim provenance into the
  existing UV results. Retain both K2 and K3 claim references and exact judgment
  provenance where the schema requires them.
- Preserve the dependency graph established at K2 and leave the K1 program
  topology unchanged.
- Preserve all scope qualifications, including that the theorem concerns only
  two separately relaxed scalar UV functionals, does not prove capacity or full
  UV tensorization, and the README decimal approximation is inaccurate while
  the exact expression is correct.
- Use content operations for the existing UV results, owning UV program, and
  affected ancestors through root. Perform no topology operation unless a
  genuinely new semantic entity is required; the gold expects none.

## Hard failures

- Nesting K2 under the K1 portfolio merely because it cites K1 evidence.
- A generic root-level converse wrapper, a separate program per K2 result, or a
  duplicate K3 program/result.
- Dropped K2 provenance at K3, altered exact values, or stronger mathematical
  claims than the accepted assessments.
- A deterministic reducer failure or violation of the precommitted operation
  contract, even if a looser validator accepts the transition.
