## `bssc-sum-capacity/theorem9-source-bound-audit`

**Verdict: indeterminate**

**Required declared-reference dependencies:** none. Gohari–Liu–Nair Theorem 9 is expressly assumed as a conditional primary-source premise rather than proved here. Consequently, this contribution does not independently establish the theorem’s coding-theoretic converse. No declared reference transaction is needed for the subsequent conditional algebraic arguments.

### What the terminal attestation establishes

The pinned execution successfully establishes that:

- the executed verifier read a 255268-byte file with SHA-256  
  `24c4153530008f7ae339ac19ca8cb90fb8ea574ea8fbcd6a36c2221722d651fa`;
- the verifier’s bounded PDF decoder produced text agreeing exactly with the canonical text generated from `theorem9_spec.json` for:
  - the theorem header checked by the script,
  - equations (19a)–(19p),
  - the displayed factorization,
  - both side conditions;
- expansion of the minima and splitting of the two interval conditions produced 30 rows whose normalized information-term expressions agree with those generated by `make_path_rows`;
- the structured specification contains the audited distinct output-bearing term sets of sizes \(3,12,12,3\) for \(Y,G,K,Z\), respectively.

The attestation does not execute or certify the later marginalization and \(V_0\) arguments; those require mathematical review.

### Verified mathematical parts

1. **Expansion to 30 rows.**  
   The six three-branch minima contribute \(18\) rows, the two two-branch minima contribute \(4\), equations (19m)–(19p) contribute \(4\), and the two double inequalities contribute \(4\), totaling \(30\). For finite mutual-information quantities,
   \[
   L\le A+\min_i b_i
   \quad\Longleftrightarrow\quad
   L\le A+b_i\quad\text{for every }i,
   \]
   and \(0\le L\le R\) is equivalent to \(L\ge0\) and \(R-L\ge0\). Thus the stated scalar expansion is mathematically correct.

2. **Input-only product marginalization.**  
   Under the displayed factorization, for every auxiliary subtuple \(D\) appearing in a row,
   \[
   p(d,x,g)=p_X(x)p_{D|X}(d|x)\bar T_{G|X}(g|x),
   \]
   and analogously for \(K\). Hence replacing an arbitrary
   \(T_{G,K|X,Y,Z}\) by
   \[
   T'_{G,K|X,Y,Z}
   =\bar T_{G|X}\bar T_{K|X}
   \]
   preserves every joint marginal needed for a term involving \(G\) alone or \(K\) alone. The displayed system contains no joint \((G,K)\) term and no output conditioned on another output. Therefore all listed equations and side conditions are preserved term by term. The reverse inclusion is immediate because an input-only product channel is an admissible auxiliary channel.

3. **Optimization inequalities.**  
   The \(Q\)-supported feasible set is a restriction of the feasible set defining \(V(1/2;G,K)\), so, including the empty-set convention,
   \[
   V_Q(G,K)\le V(1/2;G,K).
   \]
   Since \(q=1/2\) is included in the supremum defining \(B\),
   \[
   V(1/2;G,K)\le \sup_{q\in[0,1]}V(q;G,K)=B(G,K).
   \]

4. **Dependence of the \(Q_0\) rows.**  
   For \(S-X-A\) at the fair prior,
   \[
   I(S;A)=J_A(1/2)-\mathbb E J_A(q_S),\qquad
   I(X;A\mid S)=\mathbb E J_A(q_S).
   \]
   Consequently,
   \[
   I(U;A\mid W)
   =\mathbb E J_A(q_W)-\mathbb E J_A(q_{U,W}),
   \]
   with the analogous identity for \(V\). These identities cover every audited term type. On
   \(Q_0=\{0,\tfrac12,1\}\), one has \(J_A(0)=J_A(1)=0\), so receiver-channel dependence enters only through \(J_A(1/2)\). For the BSSC,
   \[
   J_Y(1/2)=J_Z(1/2)=h_2(1/4)-1/2=c.
   \]
   Thus, for a fixed hierarchy, every row depends on the four receiver channels only through \((c,g,k,c)\), and hierarchies feasible for one pair of channels remain feasible for another pair with the same \(g,k\).

5. **Finiteness of \(V_0\).**  
   The choice \(W_a=W_b=W_c=X\), with all \(U_j,V_j\) constant, is \(Q_0\)-supported, makes both side conditions zero, and permits \(R_1=R_2=0\). Hence the feasible set is nonempty. The branch-zero individual-rate rows give
   \[
   R_1\le I(U_a,W_a;Y)\le H(Y)\le1,\qquad
   R_2\le I(V_c,W_c;Z)\le H(Z)\le1.
   \]
   Therefore \(0\le V_0(g,k)\le2\), and the value is finite on its natural domain of realizable midpoint pairs.

### Material unresolved evidence obligation

The source-fidelity component is not fully certified by the supplied verifier. The PDF extractor handles literal strings shown through `Tj` and `TJ`, but it does not fail closed on all other mechanisms by which a PDF page can display or semantically encode text, including the quote text-showing operators, text inside invoked form XObjects, or other unsupported content operators. It also does not assert an exhaustive operator/resource whitelist for the two page streams. Consequently, the successful comparison proves equality with the text produced by this bounded decoder, but does not by itself affirmatively prove that the decoder captured every displayed theorem component in the pinned PDF.

Likewise, the hash and byte count authenticate the committed bytes internally but do not independently establish the provenance adjective “official”; the attested command made no network request or source-signature check.

Because source faithfulness is an express, all-or-nothing part of the declared claim, these unresolved source-extraction and provenance obligations prevent a **valid** verdict. No decisive mathematical error was found in the row expansion, marginalization, optimization inequalities, or \(Q_0\) reduction, so **invalid** is not warranted.
