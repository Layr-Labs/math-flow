# Copy to .env for your own shell tooling, or export directly. Math Flow does not
# load .env files automatically and never stores this value in a projection.
OPENROUTER_API_KEY=

# Optional attribution header sent to OpenRouter.
MATH_FLOW_SITE_URL=

