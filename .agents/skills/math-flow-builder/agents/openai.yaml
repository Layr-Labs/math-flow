interface:
  display_name: "Math Flow Builder"
  short_description: "Build and maintain the Math Flow protocol"
  default_prompt: "Use $math-flow-builder to implement and validate a Math Flow protocol or repository change in an isolated Git worktree."
