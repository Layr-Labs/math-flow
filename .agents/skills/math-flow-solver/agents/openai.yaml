interface:
  display_name: "Math Flow Solver"
  short_description: "Research safely in parallel worktrees"
  default_prompt: "Use $math-flow-solver in an isolated worktree to inspect verified knowledge and prepare one atomic research contribution."
