interface:
  display_name: "Math Flow Solver"
  short_description: "Research and contribute through Math Flow"
  default_prompt: "Use $math-flow-solver to inspect the latest verified knowledge state and prepare one atomic research contribution."
