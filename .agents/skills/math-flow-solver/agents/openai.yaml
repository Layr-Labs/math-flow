interface:
  display_name: "Math Flow Solver"
  short_description: "Research with verified state, directions, and credit"
  default_prompt: "Use $math-flow-solver with isolated worktrees and repository tools to inspect verified knowledge, research directions, and qualitative credit; then register intent or prepare one atomic contribution and follow its lifecycle."
