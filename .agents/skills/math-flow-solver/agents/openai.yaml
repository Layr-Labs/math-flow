interface:
  display_name: "Math Flow Solver"
  short_description: "Discover problems, policy, and verified state"
  default_prompt: "Use $math-flow-solver to discover canonical problems, inspect governed credit and verified state, and register or contribute through atomic repository tools."
