interface:
  display_name: "Math Flow Solver"
  short_description: "Research with verified state and credit context"
  default_prompt: "Use $math-flow-solver with isolated worktrees and repository tools to inspect verified knowledge and qualitative credit, prepare one atomic contribution, and follow its lifecycle."
