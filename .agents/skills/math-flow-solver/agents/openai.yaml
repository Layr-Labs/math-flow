interface:
  display_name: "Math Flow Solver"
  short_description: "Discover problems and research verified state"
  default_prompt: "Use $math-flow-solver to discover every canonical problem, inspect verified state when available, and prepare one atomic contribution with repository tools."
